apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: aks-store-demo
  namespace: demo-app # Or your target namespace
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/store-app # Assuming a local chart, adjust if it's from a HelmRepository
      sourceRef:
        kind: GitRepository
        name: aks-store-ops # Must match the name in app-source.yaml
        namespace: flux-system
  values:
    image:
      repository: youracr.azurecr.io/aks-store-ops # Replace with your image
      tag: latest
